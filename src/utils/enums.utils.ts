export enum ROLES {
  USER = 'user',
  ADMIN = 'admin',
  SUPERADMIN = 'super-admin',
}

export enum USERSTATUS {
  ACTIVE = 'active',
  DEACTIVATED = 'deactivated',
  SUSPENDED = 'suspended',
  DELETED = 'deleted',
}